<!DOCTYPE html>
<html>
	<head>
	  <title><%= gen_title(yield(:title))%></title>
	  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	  <%= csrf_meta_tags %>
	  <%= render 'layouts/shim' %>
	</head>
	<body class='<%= yield(:body_class) %>'>
		<%= render 'layouts/header'%>
		<div class="container">
			<% flash.each do |key, value| %>
 				<div class="alert alert-<%= key %>"><%= value %></div>
			<% end %>
			
			<%= yield %>
			<div>
				<%= debug(params) if Rails.env.development? %>
				<%= request.ip if Rails.env.development?%><hr>
				<%= session.inspect if false && Rails.env.development? %><hr>
				<%= request.env if false && Rails.env.development? %>
			</div>
		</div>
		<!--<footer class="navbar-inverse navbar-fixed-bottom">
			<nav class="navbar-inner">
    			<div class="container">
    				<div id="mainNavBar" class="nav-collapse collapse">
		     			<ul class="nav pull-left">
		     				<li class="nav-item">
		     					Delta Kappa Epsilon - Sigma Tau Chapter
		     					<br>403 Memorial Drive
		     					<br>Cambridge, MA 02139
		     				</li>
		     			</ul>
						<ul class="nav pull-right">
							<li class="nav-item">
		     					Test
		     				</li>
		     			</ul>
    			</div>
    		</nav>
		</footer>-->
		<div class="footer">
			<script type="text/javascript">
				tables = document.getElementsByClassName("table")
				new FixedHeader( tables[0] );
				new FixedHeader( tables[1] );
			</script>
		</div>
	</body>
</html>
